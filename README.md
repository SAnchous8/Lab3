# Смирнов Александр ИТС-2 Лабораторная №3

# Задание 1

### Текст задачи

Решить задачу из лабораторной работы №2 (Задание 1. List.map) для
последовательности

### Алгоритм решения

1. Попадаем в функцию main - точку входа программы

2. Запрашиваем у пользователя количество чисел для ввода:

   Выводим сообщение "Сколько чисел будете вводить?"

   Считываем введенное значение в переменную count

3. Создаем последовательность numbers, используя генератор последовательности:

   Запускаем цикл for от 1 до count

   Для каждого значения i вызываем функцию readNumber с аргументом i

   Результат каждого вызова добавляется в последовательность

4. В функции readNumber:

   Выводим приглашение "Число {index}:"

   Считываем введенное пользователем число в переменную input

   Проверяем корректность ввода:

   Если input меньше или равно нулю, выводим сообщение "Ошибка, число должно быть натуральным" и возвращаем -1

   Если input больше нуля, возвращаем введенное число

5. Применяем функцию minDigit к каждому числу из последовательности numbers:

   Для каждого числа вызывается функция minDigit

6. В функции minDigit:

   Проверяем, равно ли число нулю:
   
   Если n равно 0, сразу возвращаем 0
   
   Иначе вызываем вспомогательную рекурсивную функцию getDigits:
   
   Передаем в нее число n и начальное значение минимума 9
   
   В функции getDigits:
   
   Если num равно 0, возвращаем накопленное значение currentMin
   
   Иначе:
   
   Получаем последнюю цифру числа: digit равно num mod 10
   
   Находим минимум между digit и currentMin
   
   Рекурсивно вызываем getDigits с num деленным на 10 и новым минимумом

7. Выводим результаты:

   Выводим сообщение "Исходная последовательность:"
   
   Выводим результат применения minDigit ко всем числам

8. Завершаем программу с кодом возврата 0

### Тестирование
<img width="1006" height="645" alt="Image" src="https://github.com/user-attachments/assets/76772783-5326-4eff-b8f4-1b0fb5b52294" />
<img width="1021" height="652" alt="Image" src="https://github.com/user-attachments/assets/7a361443-05f9-451a-ad92-92cb9ab03947" />



# Задание 2

### Текст задачи

Решить задачу из лабораторной работы №2 (Задание 2. List.fold) для
последовательности

### Алгоритм решения

### Алгоритм решения

1. Попадаем в функцию main - точку входа программы

2. Запрашиваем у пользователя количество чисел для ввода:

   Выводим сообщение "Сколько чисел будете вводить?"

   Считываем введенное значение в переменную count

3. Создаем последовательность numbers, используя генератор последовательности:

   Запускаем цикл for от 1 до count

   Для каждого значения i вызываем функцию readNumber с аргументом i

   Результат каждого вызова добавляется в последовательность

4. В функции readNumber:

   Выводим приглашение "Число {index}:"

   Считываем введенное пользователем число и возвращаем его

5. Запрашиваем у пользователя цифру для поиска:
   
   Выводим сообщение "Какую цифру ищем?"

   Считываем введенное значение в переменную targetDigit

6. Проверяем корректность введенной цифры:

   Если targetDigit находится в диапазоне от 0 до 9 включительно:
   
   Выводим сообщение "Последовательность"
   
   Продолжаем выполнение программы
   
   Иначе:
   
   Выводим сообщение "Ошибка: цифра должна быть от 0 до 9"
   
   Завершаем программу с кодом возврата 0

7. Вычисляем результат с помощью свертки последовательности:

   Используем функцию Seq.fold с начальным значением 0
   
   Для каждого числа из последовательности numbers:
   
   Вызываем функцию containsDigit с текущим числом и targetDigit
   
   Прибавляем результат (0 или 1) к аккумулятору
   
   Сохраняем итоговое значение в переменную result

8. В функции containsDigit:

   Проверяем, равно ли число нулю:
   
   Если number равно 0:
   
   Проверяем, равна ли искомая цифра 0:
            
   Если digit равно 0, возвращаем 1
            
   Иначе возвращаем 0
   
   Иначе вызываем вспомогательную рекурсивную функцию checkDigits с переданным числом

   В функции checkDigits:
   
   Если num равно 0, возвращаем 0 (цифра не найдена)
   
   Иначе:
   
   Получаем последнюю цифру числа: currentDigit = num % 10
   
   Сравниваем currentDigit с искомой цифрой digit:
   
   Если равны, возвращаем 1 (цифра найдена)
   
   Если не равны, рекурсивно вызываем checkDigits с num / 10 (отбрасываем последнюю цифру)

9. Выводим результат:

   Выводим сообщение "Количество элементов, содержащих цифру {targetDigit}: {result}"

10. Завершаем программу с кодом возврата 0

### Тестирование
<img width="871" height="784" alt="Image" src="https://github.com/user-attachments/assets/dbd76335-712b-4928-9337-9fb8f3fe2b48" />
<img width="812" height="789" alt="Image" src="https://github.com/user-attachments/assets/e00a6189-7b5d-4c86-9998-2a5b4cae8401" />

